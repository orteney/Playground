<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <me.rishabhkhanna.customtogglebutton.CustomToggleButton
        android:id="@+id/topToggleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:textOff="OFF"
        android:textOn="ON"
        app:checkedColor="?attr/colorAccent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:radius="0dp"
        app:uncheckedColor="?attr/colorAccent"
        app:uncheckedTextColor="?attr/colorAccent"/>

    <me.rishabhkhanna.customtogglebutton.CustomToggleButton
        android:id="@+id/middleToggleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:textOff="OFF"
        android:textOn="ON"
        app:checkedColor="?attr/colorAccent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/topToggleButton"
        app:radius="2dp"
        app:uncheckedColor="?attr/colorAccent"
        app:uncheckedTextColor="?attr/colorAccent"/>

    <me.rishabhkhanna.customtogglebutton.CustomToggleButton
        android:id="@+id/bottomToggleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:textOff="OFF"
        android:textOn="ON"
        app:checkedColor="?attr/colorAccent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/middleToggleButton"
        app:uncheckedColor="?attr/colorAccent"
        app:uncheckedTextColor="?attr/colorAccent"/>

    <ToggleButton
        android:id="@+id/defaultToggleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:textOff="OFF"
        android:textOn="ON"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/bottomToggleButton"/>

</android.support.constraint.ConstraintLayout>